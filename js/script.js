// practice problem one (text)
// const number = prompt('write your number');
// const numberString = parseInt(number);
// const add = numberString + 200;
// alert(add)

// practice problem two (text)
const confirmMsg = confirm('Do you want to see your website link');
if (confirmMsg == true) {
    console.log("https://www.google.com/");
} else {
    console.log('hello0');
}

// practice problem three (text)
// what is JavaScritp Cookie?
// Ans: A cookie is an amount of information that persists between a server-side and a client-side

// what is the differecnce between local storage vs session storage?
// Ans: sessionStorage is similar to localStorage; the difference is that while data in localStorage doesn't expire, data in sessionStorage is cleared when the page session ends